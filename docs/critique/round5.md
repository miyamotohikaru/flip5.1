# 批評 ラウンド 5（2026-09-04）

- 対象: ブランチ `feat/mathscape`、high 1600×900（携帯は mid、実描画 663×1434）
- 撮影: `shots/r5_<定点>.png`（**15 定点**・採点はこれ）、`shots/r5m_*.png`（携帯）、一覧 `shots/r5_sheet.png`
- 前回との比較: `python3 tools/sheet.py --pair r4 r5`（`shots/r4_vs_r5.png`）
- 切り分け: `shots/r5d_*.png`（`dbg=noshadow` / `nosunocc` / `noveg` / `notrees` / `nograss` / `imponly`）、
  `shots/r5t_*.png`（`tdbg`）、`shots/r5x_*.png`（`freeze=0` の嵐 3/12/20/30 秒後・雨 6/14 秒後・夜/曇り/晴れ 10 秒後）
- 新機能・言葉: `r5_entry` `r5m_entry` `r5_hud` `r5_hud45` `r5_hint` `r5m_hint` `r5_bb10s` `r5_bb15s`
  `r5_bb_all` `r5_bb_font` `r5_lab` `r5_seed*` `r5m_flip2` `r5m_bb5s`
- 描画中のコンソール警告・エラーは 15 定点すべてで **0 件**
- 基準は R1〜R4 と同じ。AAA（RDR2 / Horizon / CoD の屋外）と並べて誤認されるか、だけを見る
- **明るさは表示線形**（`docs/CRITIC.md`）。**色みは R−G と R/G で判定し、R−B は使わない**

## 結論

| | R1 | R2 | R3 | R4 | **R5** | 差 |
|---|---|---|---|---|---|---|
| 合計（旧 12 定点） | 40/120 | 57/120 | 64/120 | 73/120 | **72 / 120**（平均 6.00） | **−1** |
| 合計（15 定点） | — | — | — | 84/150 | **85 / 150**（平均 5.67） | **+1** |
| 携帯 | 11/30 | 14/30 | 14/30 | 15/30 | **14 / 30**（golden 6 / forest 3 / night 5） | **−1** |
| ブラインド | 全「されない」 | 全「されない」 | 全「されない」 | 15/15「されない」 | **15/15「されない」** | ±0 |
| 判定 | 不合格 | 不合格 | 不合格 | 不合格 | **不合格** | |

一言で: **今ラウンドは「直した」と報告された 10 項目のうち、実際に画で確かめられたのは 3 つだけだった。**
林床の色（オレンジ砂 → 中性の褐色）、草の直線切れ（→ なめらかなフェード）、木の等間隔（畑の畝 → 不規則）は本物である。
だがそれと引き換えに、**`forest` と `ridge` と携帯の `forest` が R4 より悪くなった**。
そして**最重要と名指しした 2 つ — 150m のパゴダと、インポスターの水平な板（金床）— は、どちらも直っていない**。
`dbg=imponly` と `dbg=notrees` で切り分けたので、これは推測ではない。

いま負けている理由は R4 から入れ替わった。R4 は「地面と木の LOD」だった。R5 は:

1. **木そのもの。** 近景（5〜20m）の葉カードが**巨大な淡いミント色の台形**になり、原寸で「切った工作用紙」と読める。
   中距離（30〜150m）の木は**幹だけの棒に房を 3〜4 個刺した形**になり、樹頂から**幹が角材のように突き出て**いる。
   遠景（150m〜）は**将棋の駒／ボウリングのピンに帽子**の輪郭になった。パゴダは形を変えただけで消えていない。
2. **近景の地面が暗すぎる。** `cloudy_side` の手前の地面は表示線形 **0.0241**、空は **0.8761**。**空の 2.8%** である。
   同じ光の中で 60m 先の草地は 0.1795（空の 20%）。**同じ曇天の下で、手前の地面だけが 7.4 倍暗い。**
   境目は画面上ではっきり見える曲線で、焼け跡か油の染みに見える。`forest` の林床も同じ理由で 0.0130 まで落ち、
   黒潰れが 0.10% → **0.94%** に増えた。これは R4 に無かった**新しい破綻**である。

## 前ラウンドの誤りを 3 つ取り消す（R−B を使った判定）

`docs/CRITIC.md` の「色みの判定に R−B を使わない」を R4 の指摘に適用し直したところ、**R4 自身が 3 か所でこの誤りを犯していた**。
批評側の非を先に書く。

| R4 の指摘 | 実測（R−G / R/G） | 判定 |
|---|---|---|
| 6番「1km 先の林の隙間が錆色（マルーン）」 | R4 の時点で **R−G = −4.0 / R/G = 0.954**（緑優勢） | **誤り。錆色ではなかった。** R−B=+31 を見ていた |
| 7③「濡れた砂の赤紫が悪化（R−B +15.3→+17.5）」 | R4 の時点で **R−G = −3.0 / R/G = 0.980**、R5 は **−7.0 / 0.951** | **誤り。赤紫は最初から無い。** |
| 19番（同上） | 同上 | **誤り** |

したがって「林床の色を直した」という担当の報告のうち、**「錆色を消した」の部分は最初から存在しない問題を消したこと**になる。
ただし**林床そのものの R/G は本当に直っている**（下の答え合わせ 1 番）。両方きちんと書く。

いっぽう **嵐の空のマゼンタは本物**だった。R−G = **+21〜+27**、G が 3 チャネルの最小＝純マゼンタ。R5 でも変わっていない。

## 実測（15 定点・表示線形／下 1/3 の中央値）

| 定点 | R4 Y | R5 Y | 彩度 R4→R5 | 黒潰れ | ピンホール R4→R5 |
|---|---|---|---|---|---|
| golden | 0.1167 | 0.1205 | 0.452→0.443 | 0.00% | 17 → **17** |
| noon | 0.1723 | 0.1719 | 0.293→0.298 | 0.00% | 9 → **21** |
| dawn | 0.0512 | 0.0540 | 0.252→0.249 | 0.00% | 0 → **0** |
| cloudy | 0.2266 | 0.2335 | 0.218→0.215 | 0.00% | 0 → **0** |
| rain | 0.1353 | 0.1335 | 0.151→0.151 | 0.00% | 0 → **0** |
| storm | 0.0663 | 0.0684 | 0.073→0.073 | 0.00% | 0 → **0** |
| night | 0.0224 | 0.0237 | 0.155→0.154 | 0.28% | 0 → **5** |
| sunset_water | 0.1500 | 0.1450 | 0.243→0.251 | 0.00% | 0 → 0（マゼンタ **5**） |
| forest | 0.1443 | **0.0102** | 0.315→0.257 | **0.94%** | 0 → **28** |
| ridge | 0.0945 | **0.0676** | 0.325→0.266 | 0.03% | 37 → **35** |
| noon_side | 0.2722 | **0.1590** | 0.330→0.315 | 0.01% | 171 → **47** |
| cloudy_side | 0.1303 | **0.0285** | 0.206→0.156 | 0.19% | 243 → **206** |
| storm_live | 0.0651 | 0.0668 | 0.084→0.083 | 0.00% | — → 2 |

- `noon_side` の 0.2722 → **0.1590** は、目安（日なたの草 0.13〜0.20）の**外から中へ入った**。今ラウンドで最も正しい数値の動き。
- `forest` 0.0102 / `cloudy_side` 0.0285 は**下に行きすぎ**。理由は上記のとおり照明であって、材質ではない。
- ピンホールが 0 個なのは **15 定点中 5 つ**（dawn / cloudy / rain / storm / sunset_water）。
  担当の「8 つで 0 個」は**こちらの計測では成り立たない**。`forest` は 0 → **28**、`noon` は 9 → **21** に**悪化**している。

## 今回の 10 項目の答え合わせ

| # | 項目 | 直ったか | 今の状態（実測・切り分けつき） | 残る問題 |
|---|---|---|---|---|
| 1 | 林床（真犯人は鏡面反射） | **△** | **色は本当に直った。** `forest` の林床 R4 sRGB (104,94,65) / **R/G 1.106** → R5 **(28,31,25) / R/G 0.903**。オレンジ砂は消えた。**針葉・小枝の粒も本当に入っている**（`r5_forest` (900,600)–(1150,720) を 6 倍にすると、Y 字の小枝スプライトと短い葉のかけらが数えられる） | ① **暗すぎる。** 林床の中央値が表示線形 **0.0130**（R4 0.1443 の 1/11）、黒潰れ 0.10% → **0.94%**。担当の言う「日なたの林床 sRGB (86,94,71)」＝ Y 0.107 に相当する画素は、この定点では**見つからない**（最も明るい林床の 16px 塊で 0.0233）。② **材質そのものは直っていない。** `dbg=noveg`（草も木も消す）で測ると林床は (55,51,41) / **R/G 1.078** — R4 の 1.085 とほぼ同じ。**R/G 0.915 は照明（樹冠遮蔽＋影）で作った数字**であって、地色は赤いまま。③ 落ち葉・倒木・根・岩は**まだ 1 つも無い**。④ 追加した粒の半分が**1px 未満で点線に割れて**おり、リターでなく「死んだ画素の列」に見える。⑤ 一部の小枝が**藤色（ラベンダー）** |
| 2 | 樹冠の遮蔽（`flip_sunOcclusion` に一本化） | **△** | **効いている。** `dbg=nosunocc` との差分で `noon_side` の **94.75%** の画素が動く。林の風下に帯が出る | **開けた場所にまで効いている。** `cloudy_side` の手前 5〜20m は木が 1 本しかないのに、地面が空の **2.8%**（0.0241 / 0.8761）。60m 先の草地は 20%（0.1795）。`nosunocc` にしても 0.0418 までしか戻らず、`nosunocc`＋`noshadow` でも **0.0450**。つまり**遮蔽だけでなくもう 1 段の減光がかかっている**（鏡面を 98% 落としたぶん、曇天の環境光まで失っているのが最有力）。曇天で地面が空の 3% はありえない |
| 3 | 影に形（近カスケード 25→50m・天蓋を波打たせる） | **△** | `forest` では CSM の落ち影が**本当に地面に届いている**（`noshadow` との差で 46.98% の画素が動く）。帯の縁は R4 の直線より崩れている | ① **`noon_side` の地面には CSM の影が来ていない。** 差分画像（`noshadow` 比 6.02%）を見ると、**明るいのは木の樹冠だけ**で、草地は端から端まで真っ黒＝寄与ゼロ。手前 15〜40m の大木 2 本の足元には、**茶色い土のシールが 1 枚あるだけ**で落ち影が無い。真昼・横からの光で、これは成立しない。② `forest` の影も**先の丸い幅広の帯**で、枝の形も木漏れ日も無い |
| 4 | 「畑の畝」＝木が 8m 格子に等間隔（診断の訂正） | **○** | **訂正を受け入れる。そして直っている。** `dbg=nosunocc`（CSM だけ）にした `r5d_ns_forest` を見ると、影は**幅も間隔も不規則な広い帯**で、R4 の「平行リボン」は消えた。木の高さのばらつき（0.5〜1.42 倍）も `cloudy_side` `ridge` の稜線でひと目で分かる | 影の形が木でないことは 3 番のとおり |
| 5 | 150m の木のパゴダ（LOD1 を外す・インポスターへ一本化・アルファ膨張の金床） | **×** | — | ① **パゴダは消えていない。** `r5_storm` (900,230)–(1400,510) を 3.2 倍にすると、150m の木は**縦の棒に角材の帽子＋水平な房**、250m の木は**将棋の駒／ボウリングのピンに四角い帽子**。`storm_live` `cloudy_side` `noon_side` `ridge` すべてで出る。② **金床（水平な板）も消えていない。** `r5_noon_side` の林の足元、x=0–800 / y≈355–400 に**厚みのある黒い板が画面幅いっぱいに浮いている**。`?dbg=imponly`（インポスターだけ描く）で撮ると**同じ板がそのまま残る**ので、犯人はインポスターで確定。`cloudy_side` x=1425–1545 / y=567–585、携帯 `r5m_forest` にも複数。③ **新しい退行: 樹頂から幹が突き出る。** 中距離の木のほぼ全部で、樹冠の上に**裸の幹が角材の帽子として飛び出す**（`r5_cloudy_side` (780,200)–(1300,480) 3 倍で 6 本同時に見える）。R4 で携帯だけの欠点だったものが desktop 全域に来た。④ **中距離の木が痩せた。** `r4_ridge` と `r5_ridge` を並べると一目瞭然で、R4 は樹冠が上から下まで詰まっていたのに、R5 は**棒に房が 3〜4 個**。LOD1 を主パスから外したぶんを誰も埋めていない |
| 6 | 草の直線切れ（10m → 30m） | **○** | **直っている。** `r5_cloudy` (200,500)–(1100,620) を 3 倍にすると、草の密度がなだらかに減って消える。R4 の y≈545 の直線は無い。`dawn` も同じ | フェードの先が**のっぺりした薄緑の塗り**なのは変わらない |
| 7 | 岸（浅瀬の棚・幅 1.6〜9m・藻と沈泥・縦縞は雨裂） | **△** | **「プールの縁」は解消した。** `r5_noon` (850,430)–(1600,760) を 2.1 倍にすると、細い青灰の礫の帯・小石・水中の緑がかった沈泥が見える。R2 の 9 番から 3 ラウンドぶりの前進 | ① **縦縞は雨裂に見えない。** `dbg=noveg` で地形だけにすると、岸から**斜め上に向かって同じ向き・同じ形・ほぼ等間隔の幅広い彗星形のにじみ**が並ぶ。雨裂は下（水）に向かって集まる細い刻み目で、上に向かって広がる大きなにじみにはならない。**スマッジツールで擦った跡**に見える。② **岸線が硬い一本線**（`CRITIC.md` の落第例そのもの）。水際が 1px の濃い線でくっきり縁取られている |
| 8 | 携帯の裏返し UI | **○** | **今ラウンドで最も成功した項目。** `r5m_flip2` を実測すると、上の HUD 帯 ≈10%＋たたまれた「地形 TERRAIN ＋」カード ≈3%＋「湖 LAKE」ふだ 1 枚 ≈4%＋下のヒント/ジャイロ/ボタン ≈8% で、**文字と UI は画面の約 25%**（R4 は 69%）。ふだは同時に 1 枚、数式パネルは既定でたたまれ、裏返し中は天気と時刻の列が消えている。**指示 3 つとも実施済み** | 見えるようになった「裏返した世界」の中身が、白いひっかき傷と白い粒の雲のまま（下の flip_half / flip_full 参照）。ふだの箱はいまも不透明の UI 矩形＋UI フォント |
| 9 | 操作ヒント 8 秒 → 40 秒 | **○** | 4 秒後（`r5_hud`）に出ていて、45 秒後（`r5_hud45`）には消えている（同じ帯の画素差 7.75）。読む時間はある | ヒントの文字が**明るい草の上でほぼ読めない**（下端中央・灰色文字）。40 秒出しても読めないなら意味が薄い。影か細い暗帯を敷くこと |
| 10 | ピンホール（8 定点で 0 個・noon_side 47 / cloudy_side 203 が残る） | **△** | `noon_side` **171 → 47**、`cloudy_side` 243 → **206** は報告どおり | **「8 定点で 0 個」は成り立たない。** こちらの計測では 0 個は **5 定点**。`forest` が 0 → **28**、`noon` が 9 → **21**、`night` が 0 → **5** と**増えた定点が 3 つある**。`cloudy_side` の 206 個は x=400–600 に 80 個、x=1200–1400 に 44 個と偏っており、**明るい曇天の空を背にした 20〜80m の木**に集中している。代案は下の「担当が無理と報告した項目について」に書いた |

○ = 直った **4**（4・6・8・9）／△ = 半分 **5**（1・2・3・7・10）／× = **1**（5）

R4 は ○5・△6・×1 だった。**○ が 5→4 に減り、×（最重要と名指しした 5 番）が 2 ラウンド続けて残った。**
しかも 1 番・2 番の「直った」は**照明で暗くして見えなくしただけ**で、材質は R4 と同じ数字のままである。
